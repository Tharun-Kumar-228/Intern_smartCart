{
  "name": "intern-2",
  "version": "1.0.0",
  "description": "This is a complete full-stack project for a Smart Shopping Cart. The business logic runs on a central Node.js server, the frontend UI is built with React (optimized for a 4.5\"x4.5\" display), and the hardware logic runs on a Raspberry Pi using Python.",
  "main": "index.js",
  
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": "20.x"
  }
,
  "keywords": [],
  "author": "",
  "license": "ISC"
}
